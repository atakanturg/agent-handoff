# Gemini

Gemini is a v1 consumer adapter.

Run:

```bash
agent-handoff print-prompt gemini
```

Paste the prompt into Gemini CLI or another Gemini-based coding agent. Gemini should verify the repository state before trusting the handoff.
